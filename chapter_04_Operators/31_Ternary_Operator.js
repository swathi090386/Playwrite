let age = 25;

let is_swathi_will_go_to_goa = age > 18 ? "Yes, let go goa!" : "No you are minor, Not going";
console.log(is_swathi_will_go_to_goa) // Output: Yes, let go goa!

let age1 = 15;

let is_swathi_will_go_to_goa1 = age1 > 18 ? "Yes, let go goa!" : "No you are minor, Not going";
console.log(is_swathi_will_go_to_goa1) // Output: No you are minor, Not going

// Ternary = condition ? "true resul" : "false result";

let number = 10;

let result = number % 2 === 0 ? "Even" : "Odd";
console.log(result); // Output: Even

let score = 85;
let grade = score >= 90 ? "A" : score >= 80 ? "B" : score >= 70 ? "C" : score >= 60 ? "D" : "F";
console.log(grade); // Output: B

let temperature = 30;
let weather = temperature > 30 ? "Hot" : temperature >= 20 ? "Warm" : "Cold";
console.log(weather); // Output: Warm

let is_raining = true;
let activity = is_raining ? "Stay indoors and read a book." : "Go outside and enjoy the sunshine.";
console.log(activity); // Output: Stay indoors and read a book.


/*let is_raining = false;
let activity = is_raining ? "Stay indoors and read a book." : "Go outside and enjoy the sunshine.";
console.log(activity); // Output: Go outside and enjoy the sunshine.
*/


